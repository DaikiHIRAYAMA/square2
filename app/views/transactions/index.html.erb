<table>
  <thead>
    <h2>貸し借りしたユーザ一覧</h2>
  </thead>
  <tbody>
    <% @transactions.each do |transaction| %>
    <tr>
      <td>
        <%= link_to transaction.target_user(current_user).name, user_path(transaction.target_user(current_user).id)%>
      </td>
    </tr>
    <% end %>




  </tbody>
</table>